{
  "name": "padel-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/tournament-engine -w packages/rating-engine && npm run dev -w apps/web",
    "build": "npm run build -w packages/tournament-engine -w packages/rating-engine -w apps/web",
    "lint": "npm run lint -w apps/web",
    "test": "npm run test -w packages/tournament-engine -w packages/rating-engine"
  }
}
