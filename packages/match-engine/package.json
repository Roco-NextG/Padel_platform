{
  "name": "@padel-platform/match-engine",
  "version": "0.1.0",
  "description": "Match Engine: validacion de resultados, flujo de confirmacion, maquina de estados. Logica pura (ver 09_TECHNICAL_ARCHITECTURE.md).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
